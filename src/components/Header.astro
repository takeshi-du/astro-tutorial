---
const baseUrl = import.meta.env.BASE_URL;
---
<header class="bg-blue-600 text-white p-4 shadow-md">
  <div class="container mx-auto flex justify-between items-center">
    <a href={`${baseUrl}/`} class="text-2xl font-bold hover:text-blue-200">My Astro Blog</a>
    <nav>
      <a href={`${baseUrl}/`} class="px-3 hover:text-blue-200">ホーム</a>
      <a href={`${baseUrl}/about/`} class="px-3 hover:text-blue-200">About</a>
      <a href={`${baseUrl}/tags/`} class="px-3 hover:text-blue-200">タグ一覧</a>
      <a href={`${baseUrl}/blog/`} class="px-3 hover:text-blue-200">ブログ一覧</a>
    </nav>
  </div>
</header>